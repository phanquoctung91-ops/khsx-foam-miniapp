# Bàn giao phiên 18-20/9/2026 — khsx-foam-miniapp-live

Repo: `C:\Users\LAPTOP HP\OneDrive\Tài liệu\AI\Claude\Dự án vệ tinh\khsx-foam-miniapp-live`
Nhánh hiện tại: `work/2026-09-20-pipeline-and-fixes` (KHÔNG phải main — đã tách từ detached HEAD tại v185/e195be1 để không mất commit).
Quy tắc bắt buộc của repo: đọc `CLAUDE.md` gốc repo trước khi sửa bất kỳ gì — phải lập plan, được anh Tùng duyệt, mới được code.

## 1. Đã sửa xong, đã lên LIVE, đã xác nhận qua DB

**Bug**: nạp file KHSX 19/9/2026, thiếu 9 tấm SORA15-8 (134 → 125 tấm hiện trên miniapp).

**Root cause**: RPC `khsx_reconcile_sheet_plan_impl` (Supabase) không có nhánh xử lý khi ID đơn (khoá chính, công thức `ngày|mã|ngang|dài|dày|SL`) đã tồn tại nhưng đang bị `deleted_at` (huỷ mềm) — rơi vào `continue`, mất vĩnh viễn dù dữ liệu nguồn xuất hiện lại y hệt.

**Đã làm**:
- Sửa `supabase/schema/sheet_plan_sync.sql` — thêm nhánh hồi sinh khi `v_order.deleted_at is not null` (áp dụng cho MỌI đơn huỷ trùng ID, không phân biệt tự động hay quản lý huỷ tay — đã hỏi và chốt với anh Tùng).
- Migration mới: `supabase/migrations/20260918140000_revive_soft_deleted_sheet_order_on_reimport.sql`.
- Sửa test `tools/check-sheet-sync.cjs` (test PGlite giả lập SQL thật) — pass 7/7.
- **Đã áp trực tiếp lên Supabase live** qua `supabase db query --linked -f <file migration>` (KHÔNG chạy `supabase db push` — xem mục 3 vì sao).
- Đã verify: query trực tiếp DB live, đơn `r_19_09_2026_SORA15_8_180_200_15_9_1` có `deleted_at=null`, `plan_qty=9` — đã sống lại.

Việc còn lại của bug này: **commit** 2 file đã sửa + file migration (đang ở trạng thái uncommitted trên nhánh work/2026-09-20-pipeline-and-fixes).

## 2. Đã phát hiện, CHƯA sửa — bug đang sống, cần điều tra tiếp

**Triệu chứng anh Tùng báo**: "Tổng hoàn thành trong kỳ lọc" và "tổng năng lực từng công đoạn" không khớp; nghi ngờ khi quản lý tick chọn nhân viên cụ thể cho công đoạn May/Đóng gói thì năng lực không được ghi nhận đúng người.

**Đã xác nhận bằng dữ liệu live (query trực tiếp, không đoán)**:
- `Tổng hoàn thành` = SUM(`khsx_stage_progress.quantity`) — luôn ghi đủ.
- `Tổng năng lực` = SUM(`khsx_stage_credits.quantity`) — chỉ ghi khi RPC `khsx_apply_stage_progress_v2` xác định được `worker_id` hợp lệ.
- Bắt được bằng chứng cụ thể ngày 17/9/2026, công đoạn May, bảng `khsx_stage_operations`: nhiều đơn được ghi ĐÚNG người (bao_cham/thao_vy/loan_anh) vào các giờ rải rác trong ngày, từ nhiều thiết bị khác nhau — SAU ĐÓ, đúng lúc **09:53:14 → 09:53:19 (UTC), một thiết bị duy nhất `3f2d0b91-2a9b-4d3d-af65-eff0aefeb80b` gửi lại 6 đơn khác nhau, cách nhau ~1 giây, đúng số lượng plan_qty, nhưng `completed_by_worker_id = null`** — đè mất bản ghi đã đúng người trước đó (RPC dùng `on conflict do update`, ghi đè vô điều kiện theo bản gửi sau cùng).
- Đã loại trừ: không phải thiếu quyền (tài khoản anh Tùng có đủ quyền `progress_enter_for_other`), không phải thiếu catalogue (cả 5 nhân sự May/Đóng gói hiện tại đều có `worker_id` hợp lệ, đúng công đoạn trong `khsx_workers`).
- Việc này **đã từng xảy ra trước đó 3 lần** (Loan Anh, Thảo Vy, Bảo Chăm — có 3 migration backfill cũ: `20260905020000`, `20260905030000`, `20260908090000`) nhưng chỉ được vá DỮ LIỆU quá khứ, KHÔNG chặn được tái diễn — bằng chứng 17/9 cho thấy vẫn đang xảy ra.

**Nghi vấn chính (chưa xác nhận chắc)**: một cơ chế đồng bộ lại (resync/outbox) phía client trong `index.html` — nghi ngờ hàng đầu là `flushSupabaseStageOutbox` / logic khôi phục `supabaseStageOutbox` từ `localStorage` lúc mở lại app / `scheduleMonthlySync` — gửi lại patch cũ (đúng số lượng, thiếu `worker_id`) từ 1 thiết bị, đè lên bản ghi đã đúng người.

**Việc cần làm tiếp (CHƯA làm)**:
1. Đọc kỹ các hàm nghi vấn trong `index.html` quanh `queueStagePatchForSupabase` (dòng ~4686), `sendSupabaseStageItem` (~4701), `flushSupabaseStageOutbox`, và logic khôi phục outbox từ localStorage lúc load app — tìm đúng chỗ tái tạo patch KHÔNG kèm `worker_id` rồi gửi lại hàng loạt.
2. Sau khi xác định đúng nguyên nhân, lập plan sửa theo 1 hoặc cả 2 hướng đã bàn với anh Tùng:
   - (A) Client: khi resync, phải lấy lại đúng `worker_id` đã lưu local, không gửi rỗng.
   - (B) RPC (chốt chặn cuối, nên làm): nếu patch mới có `worker_id=null` mà dòng cũ cùng số lượng đã có `completed_by_worker_id` hợp lệ → giữ nguyên, không xoá.
3. **PHẢI vào plan mode, viết plan, được anh Tùng duyệt, mới code** — đúng luật CLAUDE.md của repo.

## 3. Phát hiện phụ, cần anh Tùng biết

`supabase migration list --linked` cho thấy **6 migration local (`20260905000000` → `20260908090000`) chưa từng được đẩy lên live**, trong khi **remote lại có nhiều migration khác không tồn tại trong repo local** (`20260909093012`, `20260909094613`, `20260910014217`...) — tức đã có người apply trực tiếp qua Dashboard/SQL editor mà không commit lại file vào repo. Bằng chứng: hàm `khsx_apply_stage_progress_v2` live hiện tại (đã fetch source thật) tân tiến hơn hẳn bản mới nhất trong repo (có `khsx_has_permission`, `khsx_order_team_assignments`, `team_key` — không có dòng nào trong repo nhắc tới các thứ này).

**=> TUYỆT ĐỐI không chạy `supabase db push` trước khi rà lại xem 6 migration cũ đó có còn cần áp không** — có thể đã lỗi thời/xung đột với schema thật hiện tại, apply nhầm có thể phá hỏng logic đang chạy sống.

## 4. Dây chuyền 4-agent /ship — mới tạo, CHƯA test được

Theo yêu cầu anh Tùng (bàn giao từ phiên "Phân tích và Dự đoán"), đã tạo:
- `.claude/agents/planner.md`, `coder.md`, `tester.md`, `reviewer.md`
- `.claude/commands/ship.md`

**Đã thử `/ship` trong phiên cũ nhưng agent `planner` KHÔNG được nhận diện** — lỗi `Agent type 'planner' not found`. Nguyên nhân: danh sách subagent được harness quét lúc khởi động phiên, không tự làm mới giữa chừng dù không còn `/agents` wizard để refresh thủ công.

**Việc cần làm ở phiên mới**:
1. Xác nhận đang đứng đúng thư mục `khsx-foam-miniapp-live`.
2. Chạy thử: `/ship đổi chữ trên nút "Xong" (complete-row-btn, tab TDSX, ~dòng 8080 index.html) thành "Hoàn tất"` — việc nhỏ, chỉ để kiểm dây chuyền chạy đúng, KHÔNG phải yêu cầu thật cần làm gấp.
3. Kỳ vọng: Planner chạy, ghi ra `.bangiao/ke-hoach.md`, IN RA rồi DỪNG HẲN chờ anh Tùng gõ "duyệt" mới chạy Coder — nếu tự chạy tiếp không chờ duyệt là SAI, cần báo lại.
4. Nếu vẫn báo `Agent type 'planner' not found` — kiểm tra lại nội dung front-matter 4 file trong `.claude/agents/` có đúng định dạng không, hoặc hỏi anh Tùng cách refresh danh sách agent của bản Claude Code đang dùng.

## Nhắc luôn

- Repo dùng tiếng Việt trong code/comment/commit — giữ nguyên văn phong.
- Chưa commit gì trong phiên 18-20/9 — `git status` sẽ thấy cả sửa bug SORA lẫn 5 file agent/ship đều đang uncommitted trên nhánh `work/2026-09-20-pipeline-and-fixes`.
